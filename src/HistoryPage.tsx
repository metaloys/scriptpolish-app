export default function HistoryPage() {
    return (
      <div className="bg-white shadow rounded-lg p-6">
        <h1 className="text-2xl font-bold text-gray-900">Polish History</h1>
        <p className="mt-2 text-gray-600">
          This page will soon show a list of all the scripts you've polished.
        </p>
        {/* We will build the table that reads from `polish_history` here later. */}
      </div>
    );
  }